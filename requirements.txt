# Pinned to the same major tested locally (6.0). Render usa Python 3.12.4,
# requisito de Django 6.0. No bajar sin volver a probar.
Django>=6.0,<6.1
whitenoise>=6.6.0
gunicorn>=21.2.0
psycopg2-binary>=2.9.9
groq>=0.9.0
google-generativeai>=0.8.0
google-api-python-client>=2.0.0
google-auth-httplib2>=0.2.0
pdfplumber>=0.11.0
PyPDF2>=3.0.0
openpyxl>=3.1.0
# Exportador HTML → PDF en Python puro (sin GTK/Cairo). Usado para
# planes de clase y guías metodológicas exportables.
xhtml2pdf>=0.2.16
# Cliente HTTP para llamadas REST a la API de imágenes de Gemini.
requests>=2.31.0
# Optional: Google OAuth via django-allauth. After install, run: python manage.py migrate
django-allauth>=65.0.0  # >=65 soporta Django 6.0
